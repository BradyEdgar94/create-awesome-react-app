let chalk,fs,path,URL,ncp,promisify,Listr,createDatabase,appConfig,cloneCMS;_e7e‍.x([["createDatabaseTask",()=>createDatabaseTask]]);_e7e‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);_e7e‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_e7e‍.w("path",[["default",["path"],function(v){path=v}]]);_e7e‍.w("url",[["URL",["URL"],function(v){URL=v}]]);_e7e‍.w("ncp",[["default",["ncp"],function(v){ncp=v}]]);_e7e‍.w("util",[["promisify",["promisify"],function(v){promisify=v}]]);_e7e‍.w("listr",[["default",["Listr"],function(v){Listr=v}]]);_e7e‍.w("./sql",[["createDatabase",["createDatabase"],function(v){createDatabase=v}]]);_e7e‍.w("./app.config.js",[["default",["appConfig"],function(v){appConfig=v}]]);_e7e‍.w("./cloneCMS",[["default",["cloneCMS"],function(v){cloneCMS=v}]]);










const access = promisify(fs.access)

async function copyAPIFiles (options) {
  return await cloneCMS(options)
}

       async function createDatabaseTask (options) {
  options = {
    ...options,
    targetDirectory: options.targetDirectory || process.cwd(),
  }

  try {
    await access(`${options.targetDirectory}`, fs.constants.R_OK)
  } catch (err) {
    _e7e‍.g.console.error(`%s Error creating database: ${err}`, chalk.red.bold('ERROR'))
    process.exit(1)
  }

  console.log('Creating database');

  const tasks = new Listr([
    {
      title: 'Copy headless API files',
      task: () => copyAPIFiles(options)
    },
    {
      title: 'Create MYSQL database',
      task: () => createDatabase(options)
    },
    {
      title: 'Configure API',
      task: () => htaccessFile(options)
    }
  ])

  await tasks.run()

  return true
}

async function htaccessFile (options) {
  const markup = `
    # BEGIN WordPress
    <IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    RewriteRule ^index\.php$ - [L]
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /index.php [L]
    </IfModule>

    SetEnv DB_NAME ${options.databaseName}
    SetEnv DB_USER ${options.databaseUser}
    SetEnv DB_PASSWORD ${options.databasePassword}
    SetEnv DB_HOST ${options.databaseHost}

    # END WordPress
  `

  fs.writeFile(
    `${options.targetDirectory}/api/.htaccess`,
    markup,
    'utf8',
    err => err ? _e7e‍.g.console.log(`%s Issue writing the .htaccess file:  ${err}`, chalk.red.bold('ERROR')) : ''
  )

  _e7e‍.g.console.error(
    `%s Success! now add ${options.cmsUrl} to MAMP PRO and then visit ${options.cmsUrl}/wp-admin`,
    chalk.green.bold('DONE')
  )
}
